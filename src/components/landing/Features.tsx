"use client";

// Features: a convenient re-export so the rest of the code can import
// the Features section from a single place without knowing its real path.
export { Features } from "@/components/landing/features/FeaturesSection";